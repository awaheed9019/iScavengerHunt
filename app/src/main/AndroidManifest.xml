<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.computer.hackny">

    <application
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CreateScavHActivity"/>
        <activity android:name=".JoinScavHActivity"/>
        <activity android:name=".TeamHomeActivity" />
        <activity android:name=".ChatActivity" />
        <activity android:name=".ViewListActivity" />
        <activity android:name=".SubmitAnswerActivity" />

    </application>

</manifest>
